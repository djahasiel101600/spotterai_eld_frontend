# API Configuration
# Backend API origin only (no /api suffix - paths in code already use /api/...)
# Local: http://localhost:8000
# Production: https://your-heroku-app.herokuapp.com
VITE_API_URL=http://localhost:8000

# NOTE: Never add API keys here!
# All AI/OpenAI calls should go through the backend API
